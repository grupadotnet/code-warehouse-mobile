This package contains the data layer of the application.

It includes:
- `remote` – handling communication with external services using Retrofit (API, network requests)
- `dto` – data transfer objects used to map and structure data received from remote sources