This package contains repository implementations that handle data operations.

It provides concrete implementations of domain repository interfaces and connects the application with data sources such as remote APIs or local storage.